source 'https://rubygems.org'
#gem 'hammer_cli_foreman'

gem 'foreman_api'

group "diagram" do
  gem 'guard'
  gem 'guard-rake'

  # alt, could use erb
  # using liquid to make transition to jekyll easier
  gem 'liquid'
  gem 'tilt'
end
