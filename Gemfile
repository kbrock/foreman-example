source 'https://rubygems.org'

gem 'foreman_api'
gem 'active_hash'

group "diagram" do
  gem 'guard'
  gem 'guard-rake'
  gem 'nokogiri'

  # alt, could use erb
  # using liquid to make transition to jekyll easier
  gem 'liquid'
  gem 'tilt'
end
